<%- include('shared/header.ejs')  %> 


<body id="login">

    <div class="overlay">

        <!-- ======================Registration form================================= -->
        <div class="container pt-5">
            <div class="row">
                <div class="col-md-7 m-auto p-5">
                    <div class="form p-5 ">
                        <h2 class="text-center text-white">Registration</h2>
                        <form action="/handelSignUp" method="POST">
                            <div class="row mt-3">
                                <div class="col">
                                    <input value="<%= oldInputs?oldInputs.fname:''%>" name="fname" type="text" class="form-control inp pt-3" placeholder="First Name">
                                    <% if (errors.find(e=>e.param=='fname')) { %>
                                        <div class="alert alert-danger">
                                            invalid input
                                        </div>
                                        <% } %>
                                </div>
                                <div class="col">
                                    <input value="<%= oldInputs?oldInputs.lname:''%>" name="lname" type="text" class="form-control inp pt-3" placeholder="Last Name">
                                    <% if (errors.find(e=>e.param=='lname')) { %>
                                        <div class="alert alert-danger">
                                            invalid input
                                        </div>
                                        <% } %>
                                </div>
                            </div>
                            <input value="<%= oldInputs?oldInputs.userName:''%>" name="userName" type="text" class="form-control inp pt-3" placeholder="Username">
                            <% if (errors.find(e=>e.param=='userName')) { %>
                                <div class="alert alert-danger">
                                    invalid input
                                </div>
                                <% } %>
                            <input value="<%= oldInputs?oldInputs.email:''%>" name="email" type="email" class="form-control inp pt-3" placeholder="Email">
                            <% if (errors.find(e=>e.param=='email')) { %>
                                <div class="alert alert-danger">
                                    invalid input
                                </div>
                                <% } %>
                            <input value="<%= oldInputs?oldInputs.password:''%>" name="password" type="password" class="form-control inp pt-3 mb-3" placeholder="Password">
                            <% if (errors.find(e=>e.param=='password')) { %>
                                <div class="alert alert-danger">
                                    Minimum eight characters, at least one letter, one number and one special character</div>
                                <% } %>
                            <input name="confirmPassword" type="password" class="form-control inp pt-3 mb-3" placeholder="confirm Password">
                            <% if (errors.find(e=>e.param=='confirmPassword')) { %>
                                <div class="alert alert-danger">
                                    invalid input
                                </div>
                                <% } %>

                            <button type="submit " class="btn btn-outline-info text-white w-100">Sign Up</button>
                            <p class="text-center pb-3"> <a href="/login" class="text-white mt-3 d-block">Sign In Here</a>
                                                                    
                                    <% if (isexsit.length!=0) { %>
                                        <div class="alert alert-danger text-center" role="alert">
                                            this email is exsist
                                        </div>
                                    
                                    <% } %>

                            </p>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>


<%- include('shared/footer.ejs')  %> 
